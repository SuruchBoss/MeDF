# dependencies
node_modules/
.pnp
.pnp.js

# builds
.next/
out/
dist/
build/Release/

# generated assets (rebuilt by `npm run build`)
apps/web/public/pdf.worker.min.mjs

# runtime data: members, documents and uploads
.medf-data/
*.medf-data/

# environment
.env
.env.local
.env.*.local

# logs and caches
*.log
npm-debug.log*
.npm
.eslintcache
.turbo
*.tsbuildinfo

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# ---------------------------------------------------------------------------
# Paid-tier source code (open-core model — see docs/OPEN_CORE.md).
# These paths must never enter this public repository; `npm run guard:private`
# fails the commit and the CI build if anything below is ever staged.
# ---------------------------------------------------------------------------
**/pro-private/
**/medf-pro/
**/private-module/
**/private-modules/
.medf-pro/
*.pro.ts
*.pro.tsx
*.pro.js
*.pro.mjs
*.pro.cjs

# generated: the demo's public assets are copied from apps/web at build time
apps/demo/public/

# UX audit output (screenshots + report), regenerated by `npm run audit:ux`
.ux-audit/
