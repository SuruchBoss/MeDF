# dependencies
node_modules/
.pnp
.pnp.js

# builds
.next/
out/
dist/
build/Release/

# generated assets (rebuilt by `npm run build`)
apps/web/public/pdf.worker.min.mjs

# runtime data: members, documents and uploads
.medf-data/
*.medf-data/

# environment
.env
.env.local
.env.*.local

# logs and caches
*.log
npm-debug.log*
.npm
.eslintcache
.turbo
*.tsbuildinfo

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# The paid module is an ordinary dependency that is simply not published, so
# there is nothing here to guard against any more (docs/OPEN_CORE.md).

# generated: the demo's public assets are copied from apps/web at build time
apps/demo/public/

# UX audit output (screenshots + report), regenerated by `npm run audit:ux`
.ux-audit/
